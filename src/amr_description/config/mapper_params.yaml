slam_toolbox:
  ros__parameters:
    use_sim_time: true
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan_front
    mode: mapping
    resolution: 0.05
    map_update_interval: 1.0
